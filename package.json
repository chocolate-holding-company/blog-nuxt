{
  "name": "blog-nuxt",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "rebuild-sqlite": "npx --no -- prebuild-install --force || npm rebuild"
  },
  "dependencies": {
    "nuxt": "^4.2.2",
    "sass": "^1.97.0",
    "vite-tsconfig-paths": "^6.0.3",
    "vue": "^3.5.25",
    "vue-router": "^4.6.4"
  }
}
